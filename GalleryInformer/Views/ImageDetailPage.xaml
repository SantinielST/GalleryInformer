<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:GalleryInformer.ViewModels"
             x:Class="GalleryInformer.Views.ImageDetailPage"
             Title="ImageDetailPage">
    
    <VerticalStackLayout Padding="20" Spacing="10" >

        <Image Source="{Binding SelectedPhoto.FullPath}" 
               Aspect="AspectFit"
               HeightRequest="400"
               HorizontalOptions="Center" />

        <Label Text="{Binding FormattedDate}"
               FontSize="Medium"
               HorizontalOptions="Center"
               FontAttributes="Bold"
               TextColor="Black" />

        <Label Text="{Binding SelectedPhoto.Name}"
               FontSize="Small"
               HorizontalOptions="Center"
               TextColor="Gray" />

        <Button Text="Назад" Command="{Binding BackCommand}" BackgroundColor="#2196F3" TextColor="White" WidthRequest="150" />
        
    </VerticalStackLayout>

</ContentPage>